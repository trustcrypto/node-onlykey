require.config({
    baseUrl: './',
    paths: {
        text:"libs/text",
        nacl: "libs/nacl",
        forge: "libs/forge.min",
        crypto: "libs/webcrypto-shim",
        events: "libs/events",
    }
});
